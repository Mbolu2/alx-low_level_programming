#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: another code
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char sam;

	for (sam = 'a'; sam <= 'z'; sam++)
	{
		putchar(sam);
	}
	putchar('\n');
	return (0);
}
